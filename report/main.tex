% !TeX program = xelatex
% =============================================================================
%  BÁO CÁO KẾT THÚC THỰC TẬP
%  Hệ thống giám sát camera bằng thị giác máy tính (VisionOS)
%
%  Biên dịch:  xelatex main.tex  →  bibtex main  →  xelatex main.tex ×2
%  hoặc gọn hơn:  latexmk -xelatex main.tex
%
%  BẮT BUỘC dùng XeLaTeX hoặc LuaLaTeX (không dùng pdfLaTeX) vì tài liệu nạp
%  phông chữ hệ thống qua fontspec để hiển thị tiếng Việt có dấu.
% =============================================================================
\documentclass[a4paper,12pt]{extreport}
% Nếu máy chưa có gói extsizes, đổi dòng trên thành:
%   \documentclass[a4paper,12pt]{report}

% ── Phông chữ và ngôn ngữ ────────────────────────────────────────────────────
\usepackage{fontspec}
\usepackage{polyglossia}
\setdefaultlanguage{vietnamese}
\setotherlanguage{english}

% Các phông dưới đây có sẵn trong môi trường biên dịch của dự án.
\setmainfont{Times New Roman}
% JetBrains Mono không có sẵn trên mọi máy. Không có phông dự phòng thì fontspec
% dừng hẳn bản dựng chứ không cảnh báo suông, nên phải tự lùi về Menlo (macOS).
\IfFontExistsTF{JetBrains Mono}
  {\setmonofont{JetBrains Mono}[Scale=0.85]}
  {\setmonofont{Menlo}[Scale=0.85]}

% ── Bố cục trang ─────────────────────────────────────────────────────────────
\usepackage[
  a4paper,
  top=2cm, bottom=2cm, left=3cm, right=2cm,
  footskip=1.2cm
]{geometry}
\usepackage{setspace}
\onehalfspacing
\setlength{\parindent}{1cm}
\setlength{\parskip}{0.3em}
\setlength{\emergencystretch}{3em}

% ── Gói hỗ trợ ───────────────────────────────────────────────────────────────
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{adjustbox}
\usepackage{float}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{multirow}
\usepackage{tabularx}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{caption}
\usepackage{listings}

% Sơ đồ vẽ thẳng bằng TikZ thay vì chèn ảnh sinh từ PlantUML hay dbdiagram: chữ trong
% sơ đồ dùng chung phông và cỡ với phần còn lại của báo cáo, phóng to không vỡ nét, và
% không phụ thuộc công cụ ngoài để dựng lại.
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, fit, backgrounds, calc, shapes.multipart}

% ── Trích dẫn ────────────────────────────────────────────────────────────────
% backend=bibtex chứ không phải biber: biber là chương trình riêng phải cài thêm,
% còn bibtex đi kèm sẵn mọi bản TeX. Đổi sang biber được nếu máy có.
\usepackage[backend=bibtex, style=numeric, sorting=none]{biblatex}
\addbibresource{refs.bib}

\usepackage[hidelinks,hypertexnames=false]{hyperref}

% ── Kiểu dùng chung cho các sơ đồ ────────────────────────────────────────────
\definecolor{sdNen}{HTML}{EEF3F9}
\definecolor{sdVien}{HTML}{1F3A5F}
\definecolor{sdNhan}{HTML}{2F6F4E}
\tikzset{
  hop/.style      = {draw=sdVien, fill=sdNen, rounded corners=2pt, line width=0.5pt,
                     inner sep=5pt, align=center, font=\footnotesize},
  hopnhan/.style  = {hop, fill=sdNhan!12, draw=sdNhan},
  bang/.style     = {draw=sdVien, fill=white, rounded corners=2pt, line width=0.5pt,
                     inner sep=0pt, align=left, font=\scriptsize,
                     rectangle split, rectangle split parts=2,
                     rectangle split part fill={sdVien!15, white}},
  mui/.style      = {-{Stealth[length=4pt]}, draw=sdVien, line width=0.5pt},
  muimo/.style    = {mui, dashed},
  nhanmui/.style  = {font=\scriptsize\itshape, inner sep=1.5pt, fill=white},
  khoi/.style     = {draw=sdVien!40, dashed, rounded corners=3pt, inner sep=8pt},
}

% ── Định dạng tiêu đề chương/mục ─────────────────────────────────────────────
\titleformat{\chapter}[block]
  {\normalfont\Large\bfseries}{Chương \thechapter.}{0.6em}{\Large}
\titlespacing*{\chapter}{0pt}{0pt}{18pt}
\titleformat{\section}{\normalfont\large\bfseries}{\thesection}{0.6em}{}
\titleformat{\subsection}{\normalfont\normalsize\bfseries}{\thesubsection}{0.6em}{}

\captionsetup{font=small, labelfont=bf, textfont=it}

% ── Đầu trang, chân trang ────────────────────────────────────────────────────
\pagestyle{fancy}
\setlength{\headheight}{15pt}
\fancyhf{}
\fancyhead[L]{\small\itshape Báo cáo kết thúc thực tập}
\fancyhead[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\fancypagestyle{plain}{\fancyhf{}\fancyhead[R]{\small\thepage}
  \renewcommand{\headrulewidth}{0.4pt}}

% ── Định dạng mã nguồn ───────────────────────────────────────────────────────
\definecolor{codebg}{RGB}{248,249,251}
\definecolor{codekw}{RGB}{170,55,110}
\definecolor{codecm}{RGB}{110,120,130}
\definecolor{codestr}{RGB}{40,120,80}
\lstdefinestyle{code}{
  backgroundcolor=\color{codebg},
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{codekw}\bfseries,
  commentstyle=\color{codecm}\itshape,
  stringstyle=\color{codestr},
  numbers=left, numberstyle=\tiny\color{codecm}, numbersep=8pt,
  breaklines=true, showstringspaces=false,
  frame=single, rulecolor=\color{codecm!30},
  xleftmargin=14pt, framexleftmargin=14pt,
  captionpos=b,
  % listings không đọc được UTF-8 mặc định; khai báo các ký tự tiếng Việt hay dùng
  extendedchars=true, inputencoding=utf8,
  literate=%
    {á}{{\'a}}1 {à}{{\`a}}1 {ả}{{\h a}}1 {ã}{{\~a}}1 {ạ}{{\d a}}1
    {ă}{{\u a}}1 {â}{{\^a}}1 {đ}{{\dj}}1 {é}{{\'e}}1 {ê}{{\^e}}1
    {í}{{\'i}}1 {ó}{{\'o}}1 {ô}{{\^o}}1 {ơ}{{\o}}1 {ú}{{\'u}}1
    {ư}{{\u u}}1 {ý}{{\'y}}1 {ệ}{{\d e}}1 {ế}{{\'{\^e}}}1 {ị}{{\d i}}1
    {ộ}{{\d o}}1 {ố}{{\'{\^o}}}1 {ợ}{{\d o}}1 {ứ}{{\'u}}1 {ự}{{\d u}}1
}
\lstset{style=code}

% ── Lệnh tắt dùng trong bài ──────────────────────────────────────────────────
\newcommand{\en}[1]{\textit{#1}}                 % thuật ngữ tiếng Anh
\newcommand{\todo}[1]{\textcolor{red}{[\textbf{CẦN ĐIỀN:} #1]}}
\newcommand{\code}[1]{\texttt{\small #1}}
\newcommand{\answerline}{\noindent\rule{\linewidth}{0.4pt}\par\vspace{0.35cm}}
\newcommand{\safeincludegraphics}[2][]{%
  \IfFileExists{#2}{\includegraphics[#1]{#2}}{%
    \fbox{\parbox[c][4cm][c]{0.9\linewidth}{\centering
      Hình minh họa chưa được cung cấp\\[0.2cm]\texttt{\detokenize{#2}}}}}%
}

% =============================================================================
\begin{document}

% ── Bìa ──────────────────────────────────────────────────────────────────────
\input{chapters/00_bia}

% ── Phần đầu: đánh số La Mã ──────────────────────────────────────────────────
\pagenumbering{roman}
\setcounter{page}{1}

\input{chapters/00_loi_cam_on}

\tableofcontents
\listoffigures
\listoftables

\input{chapters/00_viet_tat}

% ── Nội dung chính: đánh số Ả Rập ────────────────────────────────────────────
\clearpage
\pagenumbering{arabic}
\setcounter{page}{1}

\input{chapters/01_gioi_thieu}
\input{chapters/02_trien_khai}
\input{chapters/03_ket_qua}

% ── Tài liệu tham khảo ───────────────────────────────────────────────────────
% Đặt TRƯỚC \appendix, nếu không danh mục sẽ bị đánh số như một phụ lục.
\clearpage
\phantomsection
\addcontentsline{toc}{chapter}{Tài liệu tham khảo}
\printbibliography[title={Tài liệu tham khảo}]

% ── Phụ lục ──────────────────────────────────────────────────────────────────
\appendix
\input{chapters/04_phu_luc}

% ── Nhận xét của đơn vị thực tập ─────────────────────────────────────────────
\input{chapters/05_nhan_xet}

\end{document}
