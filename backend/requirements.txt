# ── Web ──────────────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.9

# ── Thị giác máy tính (luồng Tiêu chuẩn) ────────────────────────────────────
ultralytics>=8.3
supervision>=0.25
opencv-python>=4.9
numpy>=1.26

# torch/torchvision: Ultralytics tự kéo về bản phù hợp với nền tảng.
# Máy có GPU NVIDIA nên cài bản CUDA theo hướng dẫn tại https://pytorch.org
torch>=2.2
torchvision>=0.17

# ── Luồng Thông minh: LocateAnything-3B + dịch Việt–Anh ─────────────────────
# Ghim transformers 4.51.x: bản mới hơn đã bỏ một số hàm mà mã nguồn của
# LocateAnything-3B còn gọi tới.
transformers==4.51.3
peft==0.14.0
accelerate==1.2.1
timm>=1.0
sentencepiece>=0.2
sacremoses>=0.1
safetensors>=0.4
huggingface_hub<1.0
pillow>=10.0
